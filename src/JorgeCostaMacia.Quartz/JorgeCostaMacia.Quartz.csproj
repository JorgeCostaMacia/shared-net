<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<Description>Quartz job-execution trace correlation — JobTrace get-or-creates the AggregateId/CorrelationId pair on the execution context, so every listener and the job itself share the same identifiers regardless of who runs first — plus WithPostgresDefaults, the clustered Postgres store configuration every host in the family repeats</Description>
		<PackageTags>quartz,scheduling,jobs,trace,correlation,postgres,ddd</PackageTags>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Quartz" />
	</ItemGroup>

</Project>
