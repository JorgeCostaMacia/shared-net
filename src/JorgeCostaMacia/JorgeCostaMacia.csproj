<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
		<!-- Placeholder for the future 'JorgeCostaMacia' meta-package (install-everything umbrella).
		     Not packable until it is configured with PackageReferences to all packages. -->
		<IsPackable>false</IsPackable>
	</PropertyGroup>

</Project>
